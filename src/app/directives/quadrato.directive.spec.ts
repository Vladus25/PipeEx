import { QuadratoDirective } from './quadrato.directive';

describe('QuadratoDirective', () => {
  it('should create an instance', () => {
    const directive = new QuadratoDirective();
    expect(directive).toBeTruthy();
  });
});
